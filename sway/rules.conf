# ╭────────────────────────────────────────────╮
# │             REGRAS DE JANELAS              │
# ╰────────────────────────────────────────────╯

# ── Forçar apps específicos a abrirem flutuando
for_window [app_id="pavucontrol"] floating enable
for_window [title="Picture-in-Picture"] floating enable

# ── Flutuar terminal flutuante (se usar o kitty floater)
for_window [app_id="kitty" title="floater"] floating enable, sticky enable, border none, opacity 0.9

# ── Aplicações que devem abrir em workspace específico
# for_window [app_id="firefox"] workspace 2
# for_window [app_id="discord"] workspace 3

# ── Janelas popup, diálogos, e modais sempre flutuando
for_window [window_type="dialog"] floating enable
for_window [window_type="modal"] floating enable

# ── Desabilitar foco automático para certos apps (exemplo)
# for_window [app_id="some-app"] focus disable

# ── Fixar bordas ou sombra (exemplo)
# for_window [app_id="some-app"] border normal

# ── Regras para gerenciar gaps e tiling em apps pesados
# for_window [app_id="code"] border pixel 2

# ── Personalize aqui suas regras ninja underground 🤘
